# PIALAX Feature Freeze Inventory — UI Overhaul

_Date: 2026-07-24 · Ticket: **PIA-030** · Author: A1 (Inventory/Explorer) · Sources: `pialax.html` (4,986 lines), `pialax-mobile.html` (5,133 lines), `index.html`, `PIALAX-PROJECT-DEFINITION.txt`, `SPRINT_PROPOSAL_2026-07-21.md`, `CLAUDE.md`_

This document is the **zero-feature-loss contract** for the UI overhaul (PLAN_UI_OVERHAUL_2026-07-24.md). Every feature row below was observed in the shipped code and MUST survive the overhaul byte-for-behavior: a diff that removes, hides, or degrades any row is a NO-GO regardless of visual quality. Rows record anchors (functions / element IDs / constants with approximate line numbers, format `pialax.html:~NNNN / pialax-mobile.html:~NNNN`), observable behaviors including edge cases, and desktop/mobile divergence. The Persistence contract (§P) and Security/infra surface (§S) are FROZEN. No redesign opinions appear in this file.

Line-number convention: mobile mirrors desktop logic at an offset of roughly +450 to +560 lines; anchors below give both. "identical" in the divergence column means logic + IDs + observable behavior match (styling naturally differs between the two files).

---

## 1. App entry / redirect (`index.html`)

- **Device-detect redirect shim** — Where: `index.html` (whole file). Anchors: inline IIFE using `navigator.userAgent` regex + `matchMedia('(pointer:coarse)')` + `max-width:820px` + `ontouchstart`; `location.replace()`. Behaviors: forwards to `pialax-mobile.html` or `pialax.html` **carrying the full query string AND hash** so share links survive the hop; `location.replace` keeps the shim out of back-button history; on JS exception falls back to `pialax.html`; `<noscript>` meta-refresh fallback to desktop; visible fallback UI (spinner `.spin`, "Opening your flight dashboard…", manual Desktop/Mobile links); light-mode support via `@media (prefers-color-scheme:light)`. Divergence: n/a (single file).

## 2. Header / stats / status / global chrome

- **App header + Live pill** — Where: sidebar top (desktop) / app header (mobile). Anchors: `#sb-top`, `h1` "PIALAX", `.ver` "Family Flight Dashboard · v3.0", `.live-pill` + `.ld` blinking dot + `#data-pill-lbl` — pialax.html:~359 / pialax-mobile.html:~922. Behaviors: `#data-pill-lbl` flips Sample↔Live and `.ld` dot amber↔green from `loadPrices` (~3643) / proxy apply/clear (~4163, ~4181). Divergence: mobile uses a `header.app-header`; mobile also keeps a hidden duplicate `#sb-top` (`display:none`) for JS parity.
- **Topbar** — Where: main column. Anchors: `#topbar`, `#tb-title` ("Routes from X" / "Family Meetup"), `#tb-sub` (full airport name + dest list), `#share-btn-top`, `#copy-link-btn`, `#pill-trip`, `#pill-src` — pialax.html:~506 / pialax-mobile.html:~1096. Behaviors: `#tb-title` switches per mode (`setMode` ~2740); `#tb-sub` shows "Loading…"→"Loading map…"→route list; pills mirror trip type + data source. Divergence: **mobile hides the whole `#main`/`#topbar` block (`display:none`) but keeps every ID for JS writes; `#copy-link-btn` is `display:none` on mobile** (Copy Link is desktop-only UI).
- **Stats bar** — Where: bottom of main. Anchors: `#statsbar`, `#s-hub`, `#s-routes`, `#s-trip`, `#s-dates`, `#s-upd`; `updateStats()` pialax.html:~4207 / pialax-mobile.html:~4353. Behaviors: hub code, route count, trip type, dates ("Next 3 months" when unset, else long-format dep→ret), "Updated <date>". Divergence: hidden on mobile (and in desktop's own ≤640px media query) but still updated by JS.
- **Mobile status bar** — Where: under header, MOBILE ONLY. Anchors: `#status` with classes `idle|loading|success|error` (CSS pialax-mobile.html:~163–196); `updateStatus()` pialax-mobile.html:~1900. Behaviors: meetup+no date → "Choose dates or a holiday to start"; solo+no date → "Pick a weekend from the cards below"; hidden once dates set (fare count folded into `#freshness-header` per PIA-declutter). Divergence: **mobile-only** (no desktop counterpart; desktop function absent).
- **Mobile error overlay** — Anchors: `#err-overlay` + inline pre-IIFE script pialax-mobile.html:~909–917 (`window 'error'` + `'unhandledrejection'` listeners appending "JS ERROR: …"/"PROMISE REJECT: …"). Divergence: **mobile-only** debug surface.
- **Mobile step-card shell** — Anchors: `.card` wrappers + `.step-label`s "Step 1 · Plan Trip" (~950), "Step 2 · Choose Dates" (~992), "Step 3 · Results" in `#results-card` (~1040), "Step 4 · Share Plan" (~1086); `#app{max-width:430px}`; `body` safe-area padding + `padding-bottom` reserving 72px for the sticky CTA; `viewport-fit=cover` meta. Divergence: **mobile-only** information architecture (desktop = sidebar + main split).
- **Mobile sticky CTA + chips** — Anchors: `.sticky-cta` wrapping `#search-btn` pialax-mobile.html:~1153; `#mobile-sticky-bar` with `#mode-chip`/`#home-chip` (`display:none`) ~1166; `initMobileControls`/`updateMobileChips`/`isMobileViewport`/`ensureMapRendersOnMobile` pialax-mobile.html:~5093–5119; `orientationchange` listener redraws map when `#map-wrap.open` ~5121. Divergence: **mobile-only** (desktop `#search-btn` lives inline in sidebar).
- **Freshness header** — Where: sidebar top strip. Anchors: `#freshness-header`; `updateFreshnessHeader()` pialax.html:~2988 / pialax-mobile.html:~3309. Behaviors: hidden when `S.prices` empty; "N fares · Fares as of <max lastVerified local time> · <n> live · <n> sample"; locked/booked entries and `FAMILY_INFO[from].booked` routes excluded from counts; entries ≤24h and ≤7d count "live", older/null count "sample". Divergence: identical.
- **Quota bar (always-on)** — Anchors: `#quota-bar`; rendered inside `updateApiCallCount()` pialax.html:~3712–3731 / pialax-mobile.html:~3871+. Behaviors: only when `PROXY_URL` set; "SerpAPI: <remaining> / <budget> remaining [· ⛔ locked]" + "<used> used · <session> session" + colored progress bar (green / amber ≥800 used / red ≥950 or locked). Divergence: identical.

## 3. Trip Watchlist home (PIA-027 / PIA-030 / PIA-031)

- **Watchlist section + portfolio header** — Where: sidebar (Step 1 card on mobile). Anchors: `#watchlist-section`; `renderWatchlist()` pialax.html:~4779 / pialax-mobile.html:~4899; `wireWatchlist()` ~4901 / ~5017; state `_watchlistCollapsed`, `_watchlistOpen{}`, `_watchlistFocusId`, `_watchlistAdding` (~4630 / ~4758). Behaviors: header "📋 Trip Watchlist" with counts "👀 n · ✅ n · 🏁 n" + PIA-030 nudge "· n need a move" (watching/planning items with empty `nextAction`) + collapse caret ▸/▾ (click header toggles collapse); cards sorted by stage order (watching→planning→booked→completed). Divergence: identical.
- **Watchlist card (closed row)** — Anchors: `.wl-row[data-wl-id]` markup in `renderWatchlist`. Behaviors: stage emoji in tinted circle, title (ellipsized), estimate text (`watchlistEst` ~4692/~4816: family → `estimateMeetupTotalFor`, solo → mock rt||ow; completed → "🏁 done"; null → "—", otherwise "≈ $N"), open caret; 2nd line = blocker emoji dots + "🎯 <nextAction>", or faint "＋ next step" hint (`.wl-next-hint`) that expands the card and focuses the next-action input (via `_watchlistFocusId`). Card border/background tinted by stage color. Divergence: identical.
- **Watchlist card (open body)** — Behaviors: stage `<select class="wl-stage">` (👀/📝/✅/🏁, `setWatchlistStage` ~4700/~4824), FAMILY/SOLO mode chip (purple/blue), route+dates line, notes `<textarea class="wl-notes">` saved on blur (`setWatchlistNotes`), PIA-030 next-action input `.wl-next` (blur-save, no re-render) + 5 blocker chips `.wl-blocker` (💰 Price / 📅 Dates / 🧑‍🤝‍🧑 Companion / 🏖 PTO / 🤔 Deciding — `BLOCKERS` ~4605/~4733; toggle re-renders; hidden when stage=completed), 🔔 reminders list, actions row: **Open ↗** (`openWatchlistTrip` ~4708/~4832 — preset-keyed items call `applyHoliday(t.key)`; custom items apply snapshot: solo sets home/dest/extraSoloDests/soloFocus + solo mode, family sets `meetupHubOverride` + meetup mode; sets dep/ret/tripType, re-renders, `loadPrices()` cache-only, `syncURL`), **🔎 Google Flights ↗** (`watchlistGFUrl` — open-jaw via `bookingUrlFor` when `gf.openjaw`), **🛂 Flighty ↗** (static flighty.com link), **✕** remove (added items only, `removeWatchlistItem`). Divergence: identical.
- **Seed trips** — Anchors: `WATCHLIST_SEED` pialax.html:~4610–4629 / pialax-mobile.html:~4738+. Behaviors: 3 seeds — `jax` (family, "JAX — Family meetup", Aug 7–9, watching, notes re sister's Sep 1 NYC move, reminders, nextAction, blockers `companion,price`), `peru` (solo, open-jaw LAX→CUZ/LIM→LAX Sep 3–13, watching, trek-booked notes BK-035216 $1,200, 3 reminders incl. passport + briefing, blockers), `london` (family, Jun 25–Jul 3, **completed**). Seeds re-hydrate every load; localStorage stores only overrides + added items. Divergence: identical.
- **"＋ New trip" quick-add (PIA-031)** — Anchors: `#wl-newtrip`, `#nt-form`, `#nt-dest`, `#nt-dep`, `#nt-ret`, `#nt-mode` + `.nt-mode-btn` (Solo|Family), `#nt-save`, `#nt-cancel`; `addNewTrip()` pialax.html:~4751 / pialax-mobile.html:~4871. Behaviors: pre-fills dest/dates/mode from current view (active hub or soloFocus/first dest); empty dest → toast "Enter a destination"; 3-letter input uppercased to an airport code, anything else kept as free text (unmodeled city e.g. Memphis); missing dep → **default Thu→Sun next weekend** (Thu-night out / Sun return rule); item `_added:true`, stage watching; solo mode adds dest to `S.extraSoloDests` so a light solo card renders; `.nt-mode-btn` is deliberately NOT `.tt-btn` so global trip-type re-syncs never touch it (CSS comment ~82–86 / ~332). Divergence: identical.
- **Add-current-view snapshot** — Anchors: `addCurrentToWatchlist()` pialax.html:~4732 / pialax-mobile.html:~4853. Behaviors: still defined (subsumed by the quick-add form pre-fill); snapshots mode/hub/dest/dates with Thu→Sun default. Divergence: identical (no visible button in either file — function retained).
- **Share plan button** — Anchors: `#wl-share` → `sharePlan()` pialax.html:~4659 / pialax-mobile.html:~4786. Behaviors: see §9. Divergence: identical.
- **Watchlist persistence + `?wl=` restore** — Anchors: `loadWatchlist()` ~4632/~4761, `saveWatchlist()` ~4673/~4800. Behaviors: seed → apply localStorage `overrides` (stage validated against `WATCHLIST_STAGES`, notes/nextAction/blockers filtered against `BLOCKERS`) → append `added` → **apply `?wl=` URL payload last (URL wins over localStorage)**, base64(encodeURIComponent(JSON)); corrupt store silently falls back to seed. Divergence: identical.

## 4. Meetup surface (strip, routes, finder, RSVP, cost split)

- **Meetup strip (hero)** — Anchors: `#meetup-strip`; `renderMeetupStrip()` pialax.html:~1316 / pialax-mobile.html:~1807; `computeRanking()` ~1231/~1728; `getActiveHub` ~1280/~1775; `pickMeetupHub`/`setMeetupHub` ~1290–1303/~1784–1795; `hubOrder=['PIA','LAX','JAX','JFK','ORD','LHR']`. Behaviors: "📍 Hub" badge (amber `.sug` when no date; strip gets `no-date` class); **hub dropdown `#ms-hub-select`** (options "CODE · City · $TOTAL RT/OW", stable hubOrder sort — not cost-sorted; onchange `pickMeetupHub` always SETS, never toggles); inline "Searching:" date pills — 📅 dep / → / 📅 ret buttons opening `openCal('dep'/'ret')`, **amber+black when empty ("Pick depart"/"Pick return"), blue+white when set**, "· N nights" count; no date → suggested next holiday label; sub-line = per-member parts breakdown + "🚗 PIA drives" for ORD hub; appends `completedTripMarker()`. Divergence: identical (mobile strip lives inside Step 3 Results card; `.ms-sep` hidden, wrapping layout).
- **Completed-trip marker** — Anchors: `completedTripMarker()` pialax.html:~1404 / pialax-mobile.html:~1882. Behaviors: scans `FAMILY_INFO[*].tripCompleted === true`, only when today is PAST the return date; green "✅ Last trip: London · Jun 25–Jul 3, 2026 · complete" banner; '' when none. Divergence: identical.
- **Ranking engine** — Anchors: `computeRanking()` ~1231/~1728. Behaviors: candidates = `familyForDate(S.depDate)` + ORD (inserted after PIA when `S.includeORD`) + `INTL_DESTS` (when `S.includeLHR`); host doesn't fly; `'out'` RSVP members excluded; PIA→ORD is drive (no fare); PIA→intl uses `piaRouteTo`; per-ticket × headcount subtotals in `parts` strings; sorted ascending; winner `savings` = runner-up minus winner. Divergence: identical.
- **Flight Breakdown cards** — Anchors: `#meetup-routes-section`, `#meetup-routes`, `#meetup-routes-dates`; `renderMeetupRoutes()` pialax.html:~1422 / pialax-mobile.html:~1920. Behaviors (all must survive):
  - Header annotation "🔍 <dates> · N nights · RT/OW", or amber "⚠ no dates selected"; empty ranking → "Pick dates or a holiday to see fares"; all-at-hub → "All family members at hub".
  - **PIA-drives card** (hub=ORD): PIA→ORD badges, "🚗 ~2.5hr drive (one car)", copy button with "Drive ~$100" text.
  - **PIA→intl card**: Direct vs 🚗 ORD toggle pills showing both prices (`pill()` ~1557/~2038; disabled side shows "—"), auto-picks cheaper; direction-aware savings text ("Direct/ORD saves $X" green, or "+$X vs <alt>" amber when the user overrode to the pricier option); ↻ reset link when overridden (`setPiaRoute(dest,null)`); "🚗 +2.5hr drive to ORD" note; LIVE/SAMPLE `src-badge` (LIVE gets "(cache)" suffix when cached); subtotal line only when headcount>1.
  - **Locked/booked variant** (PIA-009): `activePriceObj.locked` suppresses price+toggle; `completed:true` → green "✅ FLEW" + completedNote, else "🔒 LOCKED" + arrivalNote; airline · flight numbers; "📅 <bookedDep> → <bookedRet> · no fetch (saves quota)"; copy text "(flew · complete)"/"(booked)".
  - **Generic member card**: `$price` + RT/OW label; both-leg airline rows "✈ Out (stops) · ↩ Ret (stops)" (`airlineRowHtml` ~1072/~1577, `stopsTag` "(Non-stop)"/"(N stops)"); member tag (Mom & Dad / Me / Sister via `labelFor`); "★ FOCUS" green tag + green accent for `S.focusMember`; "🤔 tentative" tag + dashed border + 0.68 opacity + italic "🤔 In the loop · not really planning on going" for tentative members; "est." tag when no date; per-member **flex-date pills** (dep/ret buttons → `openMemberCal`, amber border + "✱ custom" badge + "↻ reset" via `clearMemberDatesAndRefresh` when overridden, ✏️ edit); subtotal line when headcount>1; "🔗 Book on Google Flights ↗" deep link with the member's effective dates; 📋 copy button per card (delegated `[data-copy]` handler ~4028).
  - Divergence: identical.
- **Best Meetup Window / finder (PIA-020)** — Anchors: `#meetup-finder-section`; `computeBestMeetupWeekends()` pialax.html:~1880 / pialax-mobile.html:~2334; `estimateMeetupTotalFor()` ~1962/~2398; `renderMeetupFinder()` ~2001/~2434; `applyFinderPick()` ~1985/~2420; constants `PIA_HUB_BONUS=75`, `FINDER_NWEEKS=12`. Behaviors: candidate hubs include BOTH JAX and JFK (per-weekend `familyForDate(thu)` handles the Sep-1 straddle); weekend fares = mock × `WKLY_FACTORS` rounded to $5 × headcount; **score = total − $75 when hub=PIA (displayed total unchanged)**. Always visible in meetup mode. **No dates**: 🥇 "Meet in <City> · <HUB>" card ("PIA-default" caption), Thu–Sun dates, "$total RT total for N travelers", parts, runner-up line (notes when "PIA-default beat it"), "Click to lock these dates & hub →" (sets dates + hub, `loadPrices()` cache-only, syncURL) + hover lift. **With dates**: "💡 Cheapest week to gather · next 12 wks" ("estimate" caption); "✓ You're on the cheapest week…" when curEst ≤ bestAtHub+$5; else "Your <dates> ≈ $X" vs best-week row + "Move & save ~$N →"/"Use this week →" button; "Flexible on city?" nudge + "Switch →" when the absolute-cheapest hub/week differs. All `≈`/est labels present. Hidden entirely in solo mode. Divergence: identical.
- **RSVP Coordination Board (PIA-023)** — Anchors: `S.memberStatus` (~853/~—), `memberStatusFor`/`setMemberStatus` pialax.html:~856–867 / pialax-mobile.html:~1426–1438; UI in `renderGroupCost()` `statusSel()` ~4532/~4666; URL param `ms`. Behaviors: per-member status in|tentative|out (hub resident derived as 'hosting'); selector "✅ in / 🤔 maybe / ✖ out" per flyer row; headline "👥 N of M in" + "· n maybe" + "· n out"; 'out' → excluded from totals (struck-through amount, no breakdown card, excluded from ranking); 'tentative' → still counted, flagged, keeps legacy `S.tentativeMembers` in sync for card styling; default absent = 'in'; only non-'in' statuses serialized to URL. Divergence: identical.
- **Cost Split + Host Fairness (PIA-022)** — Anchors: `#group-cost-panel`; `memberCostsFor()` pialax.html:~4460 / pialax-mobile.html:~4597; `readLedger`/`writeLedger`/`logMeetupToLedger`/`fairestNextHost` ~4479–4501/~4616–4636; `renderGroupCost()` ~4521/~4656; `#gc-log` button. Behaviors: panel only in meetup mode with a depDate; "💰 Family cost split · <HUB>" tinted by hub color; per-member rows — 🏠 host "$0", 🚗 PIA-drives "~$100", flyers "<pct>% · $subtotal"; total headline "Total · N flying · $total RT/OW"; "⚖ Hosted so far: Mom & Dad 1× · …" from ledger; "Fairest next host: <label>" (fewest hosted; tie → highest spend); "＋ Log this meetup to fairness" button appends to ledger + toast "✓ Logged to fairness ledger". Divergence: identical.
- **Group Price-Watch (PIA-025)** — Anchors: `priceSig()`/`readGroupPrices()`/`recordGroupPrice()`/`sparklineSVG()` pialax.html:~4502–4519 / pialax-mobile.html:~4637–4655; recording hook inside `loadPrices` (~3647/~3800s, live fetches only). Behaviors: history keyed by `hub|dep|ret|tripType`; one entry per day (same-day overwrites), capped at 24; panel shows "📈 Last live $X · ↓ $N / ↑ $N / no change since <MM-DD>" (green down / red up) + 74×18 sparkline (line colored red if rising, green if falling); empty history → "📈 Tap **Get Live Fares** to start watching this trip's total over time."; **never auto-fetches** — records only on explicit live fetch. Divergence: identical.

## 5. Solo cards

- **Solo section + card list** — Anchors: `#solo-section`, `#solo-cards`, `#solo-win-lbl` ("Next 90 days · Thu–Sun"); `renderSoloCards()` pialax.html:~2225 / pialax-mobile.html:~2643; `_soloLiveConfirm`, `_soloChartOpen` ~2223/~2641. Behaviors: dest list = `dests()` ∪ non-completed solo watchlist dests for this origin ∪ `S.extraSoloDests`; **unmodeled dests** (no `AIRPORTS` entry, e.g. Memphis) render a light card: headline "— · not modeled — check real fares" + GF hand-off only, no fare grid/presets/chart/live button, price headline "—"/"not modeled". Divergence: identical.
- **Card header** — Behaviors: dest code + city colored by `DC[dest]`, freshness pill (`freshnessLabel(defaultPriceObj, S.home).pillHtml` — 🔒 BOOKED honored via FAMILY_INFO), subtitle "Next 90 days · Thu–Sun · hover bar to focus arc" or "📌 Preset: <label> · or browse alts below"; right side best price + caption "live best"/"est. best"/"preset · live"/"preset · est.". Divergence: identical.
- **Cheap-week signal strip** — Behaviors: green strip "📅 Cheapest week ≈ $N **(est.)** · <Thu–Sun dates>" + prominent "🔎 See real prices on Google Flights ↗" button (fare-alert hand-off). Divergence: identical.
- **Solo decision strip (PIA-030)** — Anchors: `.solo-decision`, `.solo-next`, `.solo-blocker`. Behaviors: same next-action input + 5 blocker chips as watchlist, synced to the matched watchlist item (lazy-creates `solo-<dest>` item on first blur/click); hidden when matched trip is completed. Divergence: identical.
- **Trip Binder embed** — `renderTripBinder(binderKeyFor(dest))` inside every solo card body (see §8). Divergence: identical.
- **Peru (CUZ) card specifics** — Anchors: `TREK_INFO` ~966/~1489, `trekInfoCardHtml()` ~973/~1495, `peruFlightOptionsHtml()` ~992/~1511. Behaviors: green confirmed-trek card (🎒 Salkantay & Inca Trail, "✅ CONFIRMED · 7 DAYS", 📅 Sep 6–12, 🎟 BK-035216 · $1200 · contact Eduardo, 📍 briefing 17:00 Triunfo St 392, 🛂 passport 6+ months, "✈ Fits your flights: in CUZ Sep 3 → acclimatize → trek Sep 6–12 → out LIM Sep 13") — note trek card renders via the seeded binder timeline; A-vs-B flight comparison (A: LAX⇄CUZ RT `~$895` vs B: two one-ways LAX→CUZ + LIM→LAX `~$925`), "✓ cheaper est." badge on winner, per-option Google Flights links, "~$" estimate labels, "real winner is decided live" footnote; generic single Book link suppressed for CUZ (A/B block carries links); `bookingUrlFor` builds the open-jaw multi-city GF query. Divergence: identical.
- **Best Weekends rows** — Anchors: `.wknd-row[data-dep|data-ret|data-dest|data-est-price]`; `computeBestWeekends()` ~1843/~2306. Behaviors: top-3 Thu–Sun windows sorted by price; "Best Value" `.bv-badge` on cheapest; per-row airline lines (live per-weekend airline preferred over mock); **ORD pivot row** (`applyOrdPivot` ~2129/~2550, PIA home only): "Best Value via ORD" green badge, total = ORD fare + $100 drive, meta "ORD flight $X + $100 drive · saves $Y"; row click → **preview panel** (📌 Preview, est. price, "Lock dates 🔒" commits dates + `loadPrices()` + syncURL / "Cancel" clears) — two-step so browsing costs zero quota; second click on an already-previewing row is a no-op. Divergence: identical.
- **Trip preset chips (PIA-013)** — Anchors: `TRIP_PRESETS` pialax.html:~667 / pialax-mobile.html:~1390s (`jax-aug7` Aug 7–9 RT; `mia-jun18`; `mia-via-jax` OW + drive, `altDest:'JAX'`; `cuz-sep3`); `isPresetStale()` ~683/~1341. Behaviors: chip applies dep/ret/tripType + focuses card (altDest focuses that card); active chip filled blue with ✓; **stale chip (today > validUntil) greyed, dashed, disabled, "· trip passed" suffix + title tooltip**; active-preset banner "📌 Preset selected" with live price + freshness pill or "fetching live price…", "✕ clear" resets dates; when preset active the rolling-window rows are re-labeled "— or pick a different weekend below —" and chart dims. Divergence: identical.
- **13-week live fetch (two-step confirm)** — Anchors: `.solo-live-btn`, `.solo-live-confirm-row/-yes/-no`; `loadSoloWeeklyPrices()` pialax.html:~3662 / pialax-mobile.html:~3832. Behaviors: button "📡 Load 13 live weeks (~13 queries)" / "🔄 Refresh 13 live weeks…" only when modeled + `PROXY_URL`; **disabled with tooltip when quota-locked**; first click swaps to confirm row "Spend ~13 of <remaining> left?" Confirm/Cancel; confirm shows "Fetching 13 prices…", fetches 13 Thu–Sun windows into `S.soloBarPrices[route]`, bumps `S.apiCalls`. Divergence: identical.
- **Notes / hand-off links per card** — Behaviors: `.solo-notes` textarea blur-saves to matching watchlist item (lazy-create); actions row "🔗 Book on Google Flights ↗" (non-CUZ) + "🛂 Flighty ↗". Divergence: identical.
- **Focus interactions** — Anchors: `setSoloFocus`/`setBarFocus` pialax.html:~2707/~2718 / pialax-mobile.html:~3087/~3098. Behaviors: card mouseenter focuses (border in dest color, `.focused` shadow), dims other arcs on map; bar hover pushes that weekend's price onto the map arc label. Divergence: identical (hover-driven; on mobile relies on tap-hover emulation).

## 6. Bar charts (solo price-trend)

- **13-week bar chart** — Anchors: `renderBarChart()` pialax.html:~2165 / pialax-mobile.html:~2585; `computeWeekendsByDate()` ~2141/~2561; `.bar-chart-svg`, `.bar-chart-row`, `#chart-<dest>`. Behaviors: 13 bars date-ascending (viewBox 200×46); **winner (min price) bar green at 0.92 opacity + green dot marker above; live bars 0.65 + `.bar-live`; sample bars 0.38** (winner-dot/opacity rules preserved exactly per PIA-037); ORD ghost = dashed green outline bar when pivot cheaper; axis row "Now … +13 wks" + "— ghost = ORD route" note; hover → `setBarFocus` (map arc + label), leave clears, click sets dep/ret. Divergence: identical.
- **Collapsed disclosure** — Anchors: `.solo-chart-toggle`, `.solo-chart-wrap`, `_soloChartOpen`. Behaviors: "▸/▾ price trend (est., 13 wks)", default **closed**; SVG stays in DOM while collapsed (max-height:0) so renderBarChart still fills it; dims to 0.7 opacity when a preset is active. Divergence: identical.

## 7. Map view

- **Geo loading + CDN fallback** — Anchors: `GEO_URL` (us-atlas states-10m via cdn.jsdelivr.net), `WORLD_GEO_URL` (world-atlas countries-110m), `GEO_FALLBACK`/`WORLD_GEO_FALLBACK` bounding-box GeoJSON pialax.html:~3783–3799 / pialax-mobile.html:~3940s; `loadWorldGeo()` ~3801/~3958; boot fetch ~4936/~5051. Behaviors: CDN failure (incl. file:// null-origin CORS) → fallback geometry, `S.geoFallback`/`S.worldGeoFallback` true; **fallback renders inline text "Borders load from internet · airport positions and routes are accurate"** and skips state borders — airports/arcs still render; console.warn only. Divergence: identical.
- **US/World toggle** — Anchors: `#map-toggle`, `.mt-btn[data-view=us|world]`, `updateMapToggle()` ~3816/~3973. Behaviors: world view lazy-loads world geo; mode/preset logic auto-switches (meetup+LHR → world; jaxaug → US; peru → world); neutral boot default US. Divergence: identical.
- **Mobile map collapse** — Anchors: `#map-toggle-btn` "🗺 Show Route Map"/"🗺 Hide Route Map", `#map-wrap.open` (height 0 → 60vw/max 50vh), handler pialax-mobile.html:~4497–4507; orientationchange redraw ~5119–5126. Behaviors: map hidden by default on mobile, expands on tap, button label + `.open` styling flip, **double `redrawMap()` at 50ms + 350ms after open** (layout-settle guard; `redrawMap` bails on zero-size container). Divergence: **mobile-only** (desktop map always visible).
- **redrawMap** — Anchors: pialax.html:~3852 / pialax-mobile.html:~4009. Behaviors (must all survive):
  - Projections: `d3.geoNaturalEarth1` (world) / `d3.geoAlbersUsa` (US) with fitExtent; bails safely when container hidden/unlaid-out or projection fails.
  - Injected SVG defs keyframes: `rd` route-dash animation (`.ra`), `pulse` home-hub pulse (`.hp` green), state polygon style `.sp` (#132134 fill, hover #1a2f48); per-dest arrow markers `#arr-<code>`.
  - Background rect #060e1a; state/country borders + #2e7cf6 outline (skipped in fallback); region hover tooltip with name.
  - **Meetup mode**: animated dashed arcs member→hub in member color (PIA-drives arc = short-dash 4,4), price bubble on each arc ("$X RT/OW", "🚗 $100" for drive, "…" when missing); hub dot white r10 with pulse + "★ MEETUP" label; family dots r7 in `DC` colors (date-aware — sister's dot at JFK for post-Sep-1 dates); other airports r3 muted; hover tooltips: code, full name, city, "★ Best Meetup Hub", "To <hub>: $X RT".
  - **Solo mode**: arcs home→each dest with arrowheads; focus dimming (unfocused arcs 0.1 alpha); bar-hover overrides arc price label with that weekend's price, else best-weekend price; home dot green r9 + pulse + "⌂ HOME"; dest dots r7; hover tooltip "From $X OW · $Y RT". **Mobile-only extra**: solo map adds a `.route-price-panel` bottom-left panel ("<HOME> ROUTES" header, per-route color dot + "HOME>CODE" + "$N OW/RT", focused route full opacity, others 0.35) — pialax-mobile.html:~4142–4163; no desktop counterpart.
  - Resize listener debounced 100ms (~3834/~3991).
  - Divergence: identical logic; mobile map height/sizing differs, collapse behavior above.
- **Legend** — Anchors: `#map-leg`; `rebuildLegend()` ~3836/~3993. Behaviors: meetup — "★ Best Hub" white dot + per-family-airport dots "(family)" (date-aware JFK) + "(intl)" for LHR; solo — "Home Airport" green + "→ <dest>" per dest. Divergence: identical.
- **Tooltip machinery** — Anchors: `#tip`, `showTip`/`hideTip`/`posTip` ~3823–3831/~3980–3990; mousemove reposition on `#map-wrap`. Behaviors: edge-flip when within 240px of right / 90px of bottom. Divergence: identical.

## 8. Trip Binder (PIA-021)

- **Binder timeline** — Anchors: `renderTripBinder(key)` pialax.html:~4404 / pialax-mobile.html:~4544; `tripBookings`/`readBookings`/`writeBookings` ~4380–4386/~4520–4526; `TB_ICON`/`TB_COL` ~4371–4372; `TRIP_BOOKINGS_SEED.peru` ~4373 (3 seeded segments: LAX→Cusco flight Sep 3, Salkantay & Inca Trail trek Sep 6–12 $1200 BK-035216, Lima→LAX flight Sep 13); `binderKeyFor(dest)` (CUZ→'peru', else dest code). Behaviors: "🧳 Trip binder · timeline" section inside each solo card; segments date-sorted, color-coded by type (✈ flight #2e7cf6 / 🎒 trek #10b981 / 🏨 lodging #f59e0b / 🗺 tour #a855f7 / 🚐 transfer #06b6d4 / 📌 other #94a3b8); **gap rows "▫ N days free in <location>"; conflict rows "⚠ overlaps previous by N days" (red); "⚠ outside your flight window (dep–ret)" for non-flight segments outside the flight-bounded window**; meta line 📍 location · $cost · 🎟 conf; empty state "No bookings yet — add flights, lodging, treks…". Divergence: identical.
- **Add/remove bookings** — Anchors: `addTripBooking(key)`/`removeTripBooking(key,idx)` ~4393–4402/~4533–4543 (exposed on `window`); form ids `tb-type-<key>`, `tb-title-<key>`, `tb-start-<key>`, `tb-end-<key>` (suffixed per binder so multiple cards don't clobber). Behaviors: type select (lodging/tour/transfer/trek/flight/other with icons), title + start required (else toast "Add needs a title + start date"), end defaults to start; added segs `_added:true` get ✕ remove; persists to `pialax_bookings_v1`; re-renders solo cards. Divergence: identical.

## 9. Share / plan link

- **Share Plan (meetup text)** — Anchors: `#share-btn-top`; `shareThePlan()` pialax.html:~1735 / pialax-mobile.html:~2207; `#share-card`, `#share-card-text`, `#share-card-close`. Behaviors: builds URL params (hub/trip/mode/dep/ret [+mhub]; **never the API key**), replaceState updates address bar; WhatsApp/iMessage-ready text: "✈️ Family meetup: City (CODE)" / "📅 dates · N nights" / blank line / "TOTAL family RT flights: $N" / per-member leg lines (hosting, drive ~$100, "$per RT x N = $sub", via-ORD "[drive $100 + ORD→X $Y]", "Out: … | Ret: …" flight lines) / "Saves $N vs next-best hub." / "Live dashboard: <url>"; shows share card below map; clipboard copy w/ textarea fallback; toast "✓ Copied! Paste into iMessage or WhatsApp" or "Share text shown below the map". Divergence: identical (mobile share card lives in Step 4 card).
- **Copy Link (deep link)** — Anchors: `#copy-link-btn`; `copyShareLink()` pialax.html:~4344 / pialax-mobile.html:~4475. Behaviors: syncURL then rAF-deferred copy of `location.href`; toast "✓ Link copied · paste anywhere" (fallback shows raw link). Divergence: **button hidden on mobile** (`display:none`), function present.
- **Shareable Family-Plan link (PIA-024)** — Anchors: `sharePlan()` pialax.html:~4659 / pialax-mobile.html:~4786. Behaviors: encodes whole watchlist (overrides + added) as `?wl=` base64 param appended to current URL; copies text summary "📋 Our trip plan (PIALAX):" + one line per trip "(emoji) Title · dep→ret ≈ $est · Stage" sorted by stage + the URL; recipient restore path in `loadWatchlist` (URL wins). Divergence: identical.
- **Per-card copy buttons + toast** — Anchors: `.route-copy-btn[data-copy]`, delegated click ~4028/~4160s; `copyRouteText()` ~1707/~2179; `showShareToast()` ~1727/~2199 (`#share-toast`, aria-live, 3.2s auto-hide). Behaviors: exact copy strings per card variant (drive / locked / generic incl. Out/Ret airlines); toast "✓ Copied!". Divergence: identical.

## 10. Data/API controls, quota & freshness

- **Data Source banner + proxy connect** — Anchors: `#api-banner`, `#api-hdr`, `#data-badge`, `#api-body`, `#api-key-inp`, `#api-apply`, `#api-clear`, `.api-note`; apply/clear handlers pialax.html:~4127–4190 / pialax-mobile.html:~4280s; `restoreProxyURL` IIFE ~4105/~4265; default `PROXY_URL='https://pialax-proxy.ashwinyedavalli.workers.dev'` ~2768/~3142. Behaviors: header click toggles body; **proxy URL validated against `^https://[a-z0-9-]+(\.[a-z0-9-]+)*\.workers\.dev$`** (multi-label allowed per PIA-006; invalid → red input border + title + temporary "Invalid URL" badge, non-blocking); trailing slash stripped; valid apply persists to `pialax_proxy_url`, flips all Sample→Live indicators + `#cal-hint` to "Fares: Google Flights live", collapses body, re-syncs `/account`, `loadPrices()`; **Disconnect** clears storage + PROXY_URL + prices, reverts every label to Sample and `#cal-hint` to "Fares: sample · Google Flights"; restore-on-boot re-validates and silently removes an invalid saved value. Divergence: **mobile wraps the banner in a `<details>` "Advanced Settings" disclosure** (~1124); mobile no-proxy banner copy says "…in Advanced Settings" vs desktop "…in the sidebar".
- **Quota module** — Anchors: `QUOTA_STORAGE_KEY='pialax_serpapi_quota'`; `_quotaPeriodKey`/`readQuota`/`writeQuota`/`bumpQuota`/`isFetchLocked`/`setQuotaBudget`/`resetQuota`/`markQuotaExceeded`/`isQuotaExceeded` pialax.html:~3092–3190 / pialax-mobile.html:~3378–3437; thresholds `QUOTA_LOCK_AT=990`, `QUOTA_RED_AT=950`, `QUOTA_AMBER_AT=800`; `MAX_CALLS_PER_REFRESH=3`, `MAX_CALLS_PER_SESSION=8`, `SESSION_CALLS`. Behaviors: shape `{period:'YYYY-MM', used, budget(1000), exceeded}`; month rollover auto-resets used; legacy budget auto-migration 100→250→1000 preserving `used`; lock when exceeded, used≥990, or session≥8. Divergence: identical.
- **Quota warning banner** — Anchors: `#quota-warning-banner`; `updateQuotaWarningBanner()` pialax.html:~3197 / pialax-mobile.html:~3438. Behaviors: **no proxy** → amber "📡 Sample data mode … Current numbers are illustrative estimates."; **locked/exceeded** → red "⛔ Fetch locked — quota safety cutoff … Search disabled until plan resets."; **≥950** → red "⚠ Approaching cap — under 50 calls left"; **≥800** → amber "🟡 SerpAPI usage warning"; body line "<used>/<budget> this month · <session> this session · <remaining> until reset (<Mon D>)"; else hidden. Divergence: mobile wording of the no-proxy line (Advanced Settings), otherwise identical.
- **Search-button quota lock** — Anchors: `updateApiCallCount()` ~3732–3746/~3890s. Behaviors: at safety cutoff, `#search-btn` disabled with `data-quota-locked="1"` + explanatory title; unlocks only if it set the lock (doesn't fight in-flight fetch disabling). Divergence: identical.
- **API panel quota pill + budget controls** — Anchors: `#api-calls-lbl` ("N calls this session", hidden at 0), `#api-quota-lbl` (remaining/budget + %-tier color + progress bar + "budget reached — falling back to mock" + "✓ synced Xm ago"/"⚠ local count only"), `#api-budget-inp` (change → `setQuotaBudget`), `#api-quota-reset` (confirm() → `resetQuota`) ~4193–4205/~4340s. Divergence: identical.
- **/account sync** — Anchors: `syncSerpApiAccount()` pialax.html:~3250 / pialax-mobile.html:~3485; `_serpApiAccountLastSync`. Behaviors: `PROXY_URL?action=account`, 8s abort timeout, zero search-credit; reconciles budget/used/exceeded from `searches_per_month`/`this_month_usage`/`total_searches_left`; called on boot + proxy apply. Divergence: identical.
- **Freshness pills** — Anchors: `freshnessLabel(entry, familyCode)` pialax.html:~3039 / pialax-mobile.html:~3343; `_freshPill` ~3074/~3371. Behaviors: 🟢 LIVE (≤24h, "Xm/Xh ago") #10b981 / 🟡 CACHED (24h–7d, "Xd ago") #f59e0b / ⚪ SAMPLE (null or >7d) #94a3b8 / 🔒 BOOKED #3b82f6 (entry.locked/booked, or FAMILY_INFO booked/bookedOutbound via `familyInfoFor` so JFK resolves to the JAX entry post-move; date via `bookedDateForToday` = next relevant itinerary date). Divergence: identical.
- **Quota-safe fetch pipeline** — Anchors: `fetchFlights()` pialax.html:~2821 / pialax-mobile.html:~3184; `_flightCache`/`_flightInflight`/`FLIGHT_CACHE_TTL_MS=24h`/`_cacheKey` ~2778–2786/~3148; `persistFlightCache`/`rehydrateFlightCache` ~2792–2819/~3155–3183; `loadPrices(allowLive)` ~3504/~3689. Behaviors (FROZEN semantics):
  - **Locked-trip short-circuit** before proxy/quota/cache: returns `{live:true, locked:true, completed, completedNote, airline, flightNo:outboundFlights, bookedDep/Ret, arrivalNote, lastVerified:bookedDep}` — zero network, zero quota.
  - `PROXY_URL` empty → mock-only. Quota exceeded → mock + console.warn. 24h in-memory + localStorage cache (`cached:true` flag); in-flight dedupe by key.
  - SerpAPI google_flights params (type 1 RT / 2 OW, USD, hl=en); **api_key never sent from browser** (Worker injects it).
  - 15s AbortController timeout (no retry on timeout), one retry on 5xx/network error with 1s backoff; 429 → `markQuotaExceeded()` + mock; 403 → mock + warn; defensive parse of `best_flights[0]`/`other_flights[0]`; live result stamped `lastVerified` ISO, cached, persisted, `bumpQuota(1)`; any failure → mock.
  - `loadPrices`: **only the Get Live Fares button passes `allowLive=true`** (~4040/~4640s); all other callers render cache-or-sample with zero spend. Live path: locked short-circuit re-check, button disabled + spinner + "Searching…", meetup fan-out builds member×hub route list with per-member flex dates + ORD→intl legs, active hub prioritized; **live fan-out capped at MAX_CALLS_PER_REFRESH=3** — excess routes get mock stamped `rateLimited:true`; JFK hub added to fan-out when dep ≥ 2026-09-01; streaming repaint per resolved route (rAF-batched); status labels: "Sample Data" / "Sample · tap Get Live Fares" / "Cached fares" / "Cached + sample" / "API Error — Using Samples" / "Live (cached)" / "Live Data"; green "live-ish" dot when live fetch or all-cache render; button label ends "Get Live Fares" (cache gaps remain) or "Refresh Fares"; live meetup fetch records group price (PIA-025).
  - Divergence: identical, plus mobile additionally drives the `#status` bar (loading state during fetch; error state "Couldn't load fares — showing estimates" when every route failed).

## 11. Sync calendar (popup) & dates

- **Calendar popup** — Anchors: `#cal-bd`, `#cal`, `#cal-hdr`, `#cal-phase`, `#cprev`/`#cnext`, `#cal-leg`, `#cal-months`, `#cal-foot`, `#cal-hint`, `#cal-ok`; `renderCal()` pialax.html:~3280 / pialax-mobile.html:~3511. Behaviors: two-month grid from `today + S.calOff` (nav clamped 0–12); day cells show day number + `$` mock price colored by tier (`tier()`: Tue–Thu cheap ×1.0, Mon mid ×1.22, Fri–Sun pricey ×1.55, rounded to $5 of first-dest base OW); legend dots Cheapest/Moderate/Pricier; **past dates disabled (`.cx`, 0.25 opacity, no events)**; today underlined; selected dep/ret filled blue (`.s0`/`.s1`), in-range tint (`.inr`); hover tooltip "Est. $N OW" (desktop map-tip reuse); phase label "Select departure"/"Select return"/"Edit dates for <member>"; footer hint "Fares: sample · Google Flights" ↔ "Fares: Google Flights live" when proxy connected. Divergence: **mobile presents as a bottom sheet** (CSS ~688–760: fixed bottom, rounded top corners; desktop positions beside sidebar via getBoundingClientRect, and desktop's own ≤640px media query also bottom-sheets it).
- **Global date selection flow** — Anchors: `onDayClick` ~3317/~3548, `openCal` ~3446/~3646, `closeCal` ~3472/~3661, `#dep-btn`/`#ret-btn`/`#dep-val`/`#ret-val`, `updateDateUI` ~3487/~3671. Behaviors: dep pick → auto-advances to return phase (RT), invalidates ret ≤ dep; ret pick before dep resets dep; OW closes immediately; open jumps grid to the month being edited; close re-renders + **auto-`loadPrices()` (cache-only) when a valid dep(+ret) exists** + syncURL; one-way disables `#ret-btn` ("—"); hub override intentionally NOT cleared on close. Divergence: identical.
- **Per-member calendars (flex dates)** — Anchors: `openMemberCal` ~3397/~3609, `closeCalMember` ~3360/~3582, `clearMemberDatesAndRefresh` ~3424/~3626, calMode `memberDep:CODE`/`memberRet:CODE` in `onDayClick`. Behaviors: edits `S.memberDates[code]` (dep invalidates stale ret; ret before dep restarts); close performs **targeted refetch of only that member's route(s)** to the active hub (+ ORD→intl leg for PIA) instead of the full fan-out; clear resets to anchor + refetches with global dates; phase label "Edit dates for Sister" etc. Divergence: identical.
- **JFK transition banner + date-aware family** — Anchors: `#jfk-banner` (~390/~971); `familyForDate()` pialax.html:~744 / pialax-mobile.html:~1308; `FAMILY_INFO.JAX.moveDate='2026-09-01'`/`moveAirport='JFK'` ~716/~1330s; `familyInfoFor`/`headcountFor`/`labelFor` ~754–763/~1317–1325. Behaviors (**core invariant**): dep **2026-08-31 → sister = JAX; dep 2026-09-01 or later → sister = JFK** (`iso >= moveDate`); banner "✈️ Sister departs JFK (NYC) for these dates — she moved from JAX in Sep 2026" shows only when dep ≥ moveDate (`updateDateUI` ~3492/~3680); ranking, routes, map dots/arcs, legend, finder candidates (both JAX and JFK evaluated per weekend), and loadPrices fan-out all use the effective family; JFK maps back to Sister's label/headcount/booked flags. Family composition 4 people: PIA=2 (Mom & Dad), LAX=1 (Me), JAX/JFK=1 (Sister). Divergence: identical.
- **Trip type toggle** — Anchors: `.tt-btn[data-v=round|one]` handler ~4072/~4620s. Behaviors: one-way clears ret; syncs `#pill-trip`/`#s-trip`; clears holiday chip highlights; re-renders strip/finder/solo; auto cache-only `loadPrices` when dates valid; syncURL. Divergence: identical.
- **Home airport selector** — Anchors: `#home-sel` change handler ~4088/~4630s. Behaviors: PIA/ORD/LAX/JAX options (orange-highlighted select); clears prices + soloBarPrices, updates stats/topbar, rebuildLegend, cache-only loadPrices, syncURL; route restrictions live in `ROUTE_GRAPH` ~569/~1180s (PIA↔ORD not directly connected; LAX additionally has MIA/JFK/CUZ). Divergence: identical.

## 12. Holiday quick-picks

- **Holiday chips** — Anchors: `#meetup-only`, `#hol-row`, `.hol-btn[data-hol]` ~394–407/~975–1035; `HOLIDAYS` ~807/~1385s; date helpers `nextMemorialDay`/`nextLaborDay`/`nextThanksgiving`/`nextOccurrence` ~770–806/~1352–1387; `applyHoliday()` pialax.html:~1087 / pialax-mobile.html:~1592; `getNextHoliday()` ~1305/~1796. Behaviors: 8 chips — Memorial Day, July 4th, Labor Day, Thanksgiving, Christmas (computed, roll to next year when past; dep = Thu on/before holiday clamped to ≥today, ret = +3 days Sun, RT forced), **JAX Aug 7–9** (hub JAX, focusMember LAX, PIA tentative via memberStatus, clears member overrides, US map), **Peru Sep 3–13** (switches home→LAX, solo mode, soloFocus CUZ, clears prices, world map, rebuilds legend), **London ✓ (done)** (fixed Jun 25–Jul 3, hub LHR, seeds per-member windows JAX Jun 24–30 / LAX Jun 24–Jul 4 from `LONDON_PRESET_MEMBER_DATES` ~645/~1345s, meetup + world map; **toggle-off on second click** clears trip and removes member overrides only if still exactly the preset values). Every apply: clears focus/tentative/status, sets chip active, updateDateUI + renderMeetupStrip + syncURL + cache-only `loadPrices()`. Chips section visible in meetup mode only. Divergence: identical (mobile chips row is horizontal-scroll always; desktop only under its ≤640px media query).

## 13. Mode switching & boot

- **Mode switcher** — Anchors: `#mode-switcher`, `.mode-btn[data-mode]`; `setMode()` pialax.html:~2729 / pialax-mobile.html:~3109. Behaviors: meetup shows `#meetup-only` + `#meetup-routes-section` + `#meetup-strip`, hides `#home-airport-row` + `#solo-section`; solo the inverse; meetup auto-switches to world map when `includeLHR` (lazy world-geo load); title swap; meetup entry auto cache-only `loadPrices` when dates valid (skipped during boot); solo clears focus state; rebuildLegend + syncURL. Divergence: mobile solo label is "✈️ Solo Weekend" (desktop "✈️ Solo Mode"); mobile also calls `updateStatus`/`updateMobileChips` in its render paths.
- **Boot sequence** — Anchors: `boot()` pialax.html:~4930 / pialax-mobile.html:~5046. Behaviors: `restoreFromURL` → `loadWatchlist` → `rehydrateFlightCache` → US topo fetch w/ fallback → world geo if needed → **neutral default: no URL state + meetup + no date → US map, no preloaded trip** (finder surfaces best weekend; specific trips open from chips/watchlist) → stats/dateUI → `setMode(S.mode)` → `renderWatchlist` → `_booting=false` → `syncSerpApiAccount` (if proxy) → cache-only `loadPrices()`. `S` defaults ~820/~1420s: home PIA, tripType round, mode **meetup**, mapView world (overridden to US by neutral default), includeORD/includeLHR true. Divergence: mobile additionally exposes the `window.*` globals inside boot (desktop does it at top level ~4971) and runs `initMobileControls()` + `ensureMapRendersOnMobile()`.
- **Delegated events** — Anchors: `[data-copy]` document click ~4028; calendar nav/ok/backdrop, dep/ret buttons, search, share, mode, map-toggle, holiday, trip-type, home-sel, api handlers ~4032–4101 (mobile mirrors ~4560–4660 + `#map-toggle-btn`). Divergence: identical except mobile-only map toggle.

---

## P. Persistence contract — FROZEN

### localStorage keys (name · shape · writers/readers)

| Key | Shape | Writers | Readers | Anchors (desktop / mobile) |
|---|---|---|---|---|
| `pialax_prices_v1` | `{ "<from>\|<to>\|<dep>\|<ret>": { data:{ow,rt,live,airline,flightNo,airlineLogo,stops,returnAirline,returnFlightNo,returnAirlineLogo,returnStops,lastVerified}, ts } }` — TTL 24h, stale dropped on persist | `persistFlightCache` | `rehydrateFlightCache` | ~2791–2819 / ~3154–3183 |
| `pialax_serpapi_quota` | `{ period:"YYYY-MM", used:int, budget:int(1000), exceeded:bool }` — month rollover resets used; budget migrations 100→250→1000 | `writeQuota` (via bump/set/reset/markExceeded/syncSerpApiAccount) | `readQuota` | ~3092–3190 / ~3378–3437 |
| `pialax_proxy_url` | plain string, must match `https://*.workers.dev` (multi-label) | `#api-apply` handler; removed by `#api-clear` and on failed validation | `restoreProxyURL` IIFE | ~4104–4176 / ~4264–4324 |
| `pialax_bookings_v1` | `{ "<binderKey>": [ {type,title,start,end[,location,cost,conf,notes]} ] }` (user-added only; seeds live in code) | `writeBookings` (add/removeTripBooking) | `readBookings`/`tripBookings` | ~4370–4402 / ~4510–4543 |
| `pialax_host_ledger_v1` | `{ "<memberCode>": { hosted:int, spend:int } }` | `writeLedger` (logMeetupToLedger) | `readLedger`/`fairestNextHost` | ~4458–4501 / ~4595–4636 |
| `pialax_group_price_v1` | `{ "<hub>\|<dep>\|<ret>\|<tripType>": [ {ts:ISO, total:int} ] }` — 1/day, max 24 | `recordGroupPrice` | `readGroupPrices` (renderGroupCost) | ~4459–4511 / ~4596–4646 |
| `pialax_watchlist_v1` | `{ overrides: { "<id>": {stage,notes,nextAction,blockers[]} }, added: [full trip objects `_added:true`] }` | `saveWatchlist` | `loadWatchlist` | ~4596–4681 / ~4727–4805 |

### URL query parameters (`syncURL` pialax.html:~4298 / pialax-mobile.html:~4434; `restoreFromURL` ~4217/~4361; plus `sharePlan`/`loadWatchlist`)

| Param | Values / format | Meaning |
|---|---|---|
| `hub` | airport code in `AIRPORTS` | `S.home` |
| `trip` | `round` \| `one` | trip type (syncs buttons + pills on restore) |
| `mode` | `meetup` \| `solo` | active mode |
| `dep` / `ret` | `YYYY-MM-DD` (ret only honored for round) | anchor dates (parsed at T12:00:00) |
| `mhub` | airport code | meetup hub override |
| `ord` / `lhr` | `1` \| `0` | includeORD / includeLHR flags |
| `md` | `CODE:dep:ret\|CODE:dep:` | per-member flex-date overrides (ret optional) |
| `pia` | `DEST:direct\|DEST:via-ord` | PIA route overrides |
| `ms` | `CODE:tentative\|CODE:out` | RSVP statuses (only non-'in' serialized) |
| `wl` | base64(encodeURIComponent(JSON `{overrides,added}`)) | shared whole-plan watchlist (PIA-024; URL wins over localStorage) |

Notes: `syncURL` is rAF-debounced and uses `history.replaceState` (no reload); `_urlStateLoaded` gates the neutral boot default; no `#hash` state is currently written, but `index.html` forwards hashes and must keep doing so. `shareThePlan` writes a subset (hub/trip/mode/dep/ret[/mhub]).

---

## V. Hardcoded visual values (for A2 token coverage — representative catalog)

### CSS custom properties currently defined
- **Desktop `:root`** (pialax.html:~34–42): `--bg #07101e`, `--surface #0f1d2e`, `--surface2 #162233`, `--border #1c2f45`, `--border2 #243b55`, `--text #e8f0f8`, `--muted #5b7a99`, `--accent #2e7cf6`, `--a-dim rgba(46,124,246,.12)`, `--green #10b981`, `--amber #f59e0b`, `--red #ef4444`, `--lax/--jax/--pia/--ord`, `--cheap/--mid/--pricey`, `--r 10px`, `--ease`, `--fast`.
- **Mobile `:root`** (pialax-mobile.html:~38–71): adds spacing/radius tokens `--space-xs 6px / -sm 10px / -md 16px / -lg 24px`, `--radius-md 14px / -lg 20px`; `--text-main #e6edf6`, `--text-muted #7a99b8`, `--card #0f1d2e`, `--border #1a2d44`, `--primary #2e7cf6`, **`--green #00c853` (differs from desktop's #10b981)**; legacy aliases (`--surface`, `--surface2 #162233`, `--border2 #243b55`, `--text`, `--muted`, `--accent`, `--a-dim`, `--amber`, `--red`, airport + tier colors, `--r = var(--radius-md)`).
- ⚠ Both files' generated HTML references **`var(--ink)` and `var(--line)` which are never defined** (fall back to inherited color). A2 must either define them or map them — behavior today is "inherit".

### Distinct hex colors by frequency (grep sweep, both files similar)
Top: `#fff`(24), `#888`(11 — fallback dest color), `#10b981`(≈11), `#f59e0b`(9), `#000`(9), `#2e7cf6`(8), `#06b6d4`(7), `#a855f7`(6), `#94a3b8`(5), `#ef4444`(4–5), `#1e3550`(3), `#060e1a`(3 — map background). Singles/pairs: `#ffffff #4a6a88 #38bdf8 #2a4060 #fbbf24 #ec4899 #eab308 #e11d48 #d97706 #b8852b #b45309 #a0c4e8 #7a99b8 #6ee7b7 #5b7a99 #4a6080 #3b82f6 #334155 #253548 #243b55 #22c55e #1f2937 #1e293b #1c2f45 #1a2f48 #1a1a1a #162233 #14b8a6 #132134 #0f1d2e #07101e` (+mobile-only `#fca5a5 #7f1d1d #d6e0ec #6e93bd #1f3350 #0b1726 #00c853 #1a2d44 #e6edf6`).
Main clusters: map SVG internals (redrawMap ~3852/~4009: `#060e1a` bg, `#132134/#1e3550/#1a2f48` states, `#2e7cf6` borders, `#a0c4e8/#4a6a88/#6ee7b7` labels); `DC` airport palette (~572/~1183); freshness pill colors (`#10b981/#f59e0b/#3b82f6/#94a3b8` in `_freshPill`); `TB_COL` binder palette (~4372/~4512); `WATCHLIST_STAGES` colors (`#38bdf8/#f59e0b/#10b981/#94a3b8`) + mode chips (`#a855f7/#2e7cf6`); home-sel orange block (`#f59e0b/#fbbf24/#d97706/#b45309/#b8852b/#1a1a1a/#1f2937`); `#jfk-banner` slate set (`#1e293b/#334155/#94a3b8/#38bdf8`); sparkline `#ef4444/#10b981`.

### Type & geometry clusters
- Font sizes: desktop base 14px, mobile base 15px. Inline sizes are dominated by sub-rem steps — desktop top counts: `.55rem`(45), `.6rem`(35), `.5rem`(20), `.58rem`(19), `.62rem`(16), `.52rem`(15), `.72rem`(11), `.78rem`(9), plus `.48–.95rem` tail; h1 `1.5rem`/mobile `1.6rem`; finder price `1.1rem`. Mobile mirrors with both `.55rem` and `0.55rem` spelling variants.
- Radii: `6px`(22), `8px`(21), `5px`(13), `20px` pills(10), `3px`(7), `9px`, `7px`, `4px`, `10px`, `2px`, `11px`, `14px` (cal), `18px` (mobile sheet corners), plus tokens 14/20px on mobile.
- Motion: `blink 2.4s`, `spin .6s`, `calIn .16s`, `stripBorder 2.8s`, map `rd 1.8s` dash + `pulse 2.6s` (injected in SVG defs), transitions `.1–.3s`.
- Media queries: desktop has exactly one — `@media (max-width:640px)` (~283–351) providing its own stacked-mobile fallback (map on top 44vw, statsbar/sb-foot hidden, calendar bottom sheet, 44px tap targets). Mobile file is single-layout (no media query; `max-width:430px` shell) + safe-area env() padding.

---

## S. Security / infra surface — must remain byte-identical or equivalent

1. **CSP meta tag** — pialax.html:6 / pialax-mobile.html:6 (identical value): `default-src 'self'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' https://pialax-proxy.ashwinyedavalli.workers.dev https://serpapi.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com; font-src 'self' data:;` plus `<meta name="referrer" content="strict-origin-when-cross-origin"/>`.
2. **SRI-pinned CDN scripts** — both files, ~13–18: d3 7.9.0 `integrity="sha512-vc58qvvBdrDR4etbxMdlTt4GBQk1qjvyORR2nrsPsFPyrs+/u5c3+1Ct6upOgdZoIl7eq6k3a1UPDSNAQi/32A=="`; topojson 3.0.2 `integrity="sha512-4UKI/XKm3xrvJ6pZS5oTRvIQGIzZFoXR71rRBb1y2N+PbwAsKa5tPl2J6WvbEvwN3TxQCm8hMzsl/pO+82iRlg=="`; both `crossorigin="anonymous" referrerpolicy="no-referrer"`. Verified by `scripts/verify-sri.sh`; plus the runtime defense-in-depth IIFE checking `d3.select`/`topojson.feature` (~19–32 both files).
3. **`esc()` usage pattern** — pialax.html:~933 / pialax-mobile.html:~1461: every dynamic string interpolated into `innerHTML` goes through `esc()` (labels, notes, titles, copy payloads, locations, ledger text…); copy buttons use `data-copy` attributes + one delegated listener instead of inline `onclick` (XSS-safety note ~4027). Any new template in the overhaul must follow the same pattern (A6 audit item).
4. **PROXY_URL handling** — default `https://pialax-proxy.ashwinyedavalli.workers.dev` (~2768/~3142); empty string = mock-only mode; strict `workers.dev` https regex validation on both apply and restore; **API key never present in the browser** (Worker-injected; explicitly excluded from share URLs); localStorage override `pialax_proxy_url`; matching entry pinned in CSP `connect-src`.
5. **Structural**: single IIFE + `'use strict'` per file (~550 / ~1173); no build step; no `console.log` in shipping HTML (console.warn/info/error only — preflight-enforced); mobile pre-IIFE error-overlay script.

---

## X. Cross-check appendix

### PIALAX-PROJECT-DEFINITION.txt FEATURE SUMMARY → inventory rows

| Definition item | Status |
|---|---|
| Home airport selector (PIA, ORD, LAX, JAX) + route restrictions | ✅ §11 Home airport selector; `ROUTE_GRAPH` |
| Live pill indicator (Sample vs Live) | ✅ §2 App header + Live pill |
| SerpAPI key input, mock/live toggle | ✅ §10 Data Source banner (now proxy-URL input, key stays in Worker — evolved as documented) |
| Calendar picker (dep+ret RT, dep-only OW) | ✅ §11 Calendar popup / date flow |
| Visual price-tier indicators (cheap/mid/pricey) | ✅ §11 (`tier`, `.dp` colors, legend) |
| Trip type toggle | ✅ §11 |
| Holiday quick-pick chips (5 US holidays, Wed-dep/Sun-ret per doc) | ✅ §12 — **note**: shipped logic is Thu-on-or-before → Sun (+3), not Wed→Sun; the code is the contract |
| Family cost summary bar (total to meet at hub, cheapest highlighted) | ✅ §4 Meetup strip + ranking (evolved from 4-card bar to hub dropdown + strip; savings on winner retained) |
| Breakdown "LAX→PIA $362 + JAX→PIA $261 · RT" | ✅ §4 parts strings + Flight Breakdown cards |
| Date context line; live updates on toggle/holiday | ✅ §4 strip date pills; auto re-render/loadPrices hooks |
| Price cards (sidebar) per outbound dest: OW/RT, airline, best date, live/sample badge | ✅ §5 Solo cards |
| Drive-to-ORD row (PIA home) with 2.5-hr drive + savings green | ✅ §5 ORD pivot row; §4 PIA-drives / via-ORD toggle |
| US route map: animated arcs, markers (home pulse/dest/other), price labels, hover tooltips, state borders + CDN fallback, legend | ✅ §7 |
| Family sync calendar (two-month overlap, per-airport cheap tiers, overlap score 0–6, click-to-set + 5-day auto-return) | ⚠ **PARTIALLY NOT FOUND — verify with owner.** The shipped calendar is the two-month price-tier popup (§11): per-day $ tiers, click-to-set dep→ret, past-date disabling, selected outline, live/sample legend attribution all present. **No "overlap score 0–6" scoring/coloring and no fixed 5-day auto-return exist in current code** (RT return is user-picked; presets/finder use Thu–Sun). Best current equivalents: the finder (§4) + WKLY_FACTORS weekend model. Treat the doc's overlap-score wording as historical. |
| Data source control (expandable banner, badge Sample→Live, calendar legend attribution) | ✅ §10, §11 (`#cal-hint`) |
| Stats bar (hub, routes, trip, dates/"Next 3 months", last updated) | ✅ §2 Stats bar |
| Inline GEO_FALLBACK for file:// | ✅ §7 |
| "localStorage NOT used; in-memory only" | ❌ outdated by design — superseded by the 7-key persistence contract (§P), per PIA-005/021/022/025/027 |

### Sprint tickets PIA-020..027 (all shipped) → inventory rows

| Ticket | Status |
|---|---|
| PIA-020 Best Meetup Window | ✅ §4 finder (`computeBestMeetupWeekends`, `estimateMeetupTotalFor`, `applyFinderPick`, recommender + "Move & save" + "Switch" nudges, PIA_HUB_BONUS) |
| PIA-021 Trip Binder | ✅ §8 (timeline, gap "N days free", overlap ⚠, outside-flight-window ⚠, add/remove, Peru seed, `pialax_bookings_v1`) |
| PIA-022 Cost Split + Host Fairness | ✅ §4 (memberCostsFor %, ledger, fairest next host, log button, `pialax_host_ledger_v1`) |
| PIA-023 RSVP Coordination Board | ✅ §4 (statuses hosting/in/tentative/out, "N of M in" headline, totals gating, `ms` URL param) |
| PIA-024 Shareable Family-Plan link | ✅ §9 (`sharePlan`, `?wl=`, copy-summary text, URL-wins restore) |
| PIA-025 Group Price-Watch | ✅ §4 (priceSig history, "↓ $N since <date>", sparkline, never auto-fetches, `pialax_group_price_v1`) |
| PIA-026 Handoff principle (chore) | ✅ manifest in code as GF/Flighty hand-off links (§5, §3) — doc-only ticket, no UI row required |
| PIA-027 Trip Watchlist (stages/notes/reminders/"+ New trip") | ✅ §3 (stages 👀📝✅🏁, seed trips, notes, reminders, nextAction+blockers [PIA-030], quick-add [PIA-031], Thu→Sun default, localStorage+URL) |

### NOT FOUND flags (verify with owner)
1. **Overlap score 0–6 sync calendar** (project definition) — not present in either file; superseded by price-tier calendar + Best-Meetup-Window finder. If the overlap-score view must return, it is net-new work, not preservation.
2. **"Auto-sets 5-day return" on calendar click** (project definition) — not in code; RT return is a second explicit pick; Thu→Sun (3-day) is the default window elsewhere.
3. **Wed-departure holiday preset rule** (project definition says Wed before / Sun after) — code ships Thu-on-or-before → Sun (+3).
Everything else in both source documents maps to at least one inventory row above.

### Known desktop/mobile divergences (documented; keep or consciously reconcile per PIA-030 spec)
1. Mobile-only: `#err-overlay` error console, `#status` bar + `updateStatus()`, Step 1–4 card shell, sticky bottom CTA, `#mobile-sticky-bar` chips (+`updateMobileChips`), collapsed map behind `#map-toggle-btn`, Advanced-Settings `<details>` around the API banner, orientationchange map redraw.
2. Desktop-only: visible `#copy-link-btn` **and visible `#share-btn-top`** (mobile CSS hides `#share-btn-top` at ~877 — on mobile the only visible share triggers are the watchlist `#wl-share` button and per-card copy buttons; `shareThePlan` stays wired to the hidden button), always-visible map + `#map-toggle` position, visible `#statsbar`/`#topbar`/`#sb-foot`, its own ≤640px responsive fallback media query. Mobile-only map extra: `.route-price-panel` (solo view).
3. Token drift: mobile `--green:#00c853` vs desktop `#10b981`; mobile 15px base font vs desktop 14px; mobile spacing/radius tokens absent on desktop; both reference undefined `--ink`/`--line`.
4. Label drift: "Solo Mode" (desktop) vs "Solo Weekend" (mobile); no-proxy banner "in the sidebar" vs "in Advanced Settings".
5. Structural: mobile keeps hidden duplicates of desktop chrome (`#sb-top`, `#main`, `#topbar`, `#statsbar`, `#sb-foot`; `#sb{display:contents}`) purely as JS write-targets; `#mobile-sticky-bar`/`#mode-chip`/`#home-chip` are commented "Legacy … IDs preserved" and updated by `updateMobileChips()` (gated on `isMobileViewport()` in `setMode`); mobile exposes `window.*` globals inside `boot()` rather than at IIFE top level; hover-driven handlers (card focus, map/calendar tooltips) are desktop-mirror carryovers with no touch equivalent on mobile.
